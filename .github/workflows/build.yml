echo "Starting build..."
echo "Compiling..."
# This line was causing the failure. Replace with the actual build/compilation command for the project.
# For example, if it's a C/C++ project: make build
# If it's a Java Maven project: mvn clean install
# As a placeholder for a successful compilation:
echo "Project compiled successfully."