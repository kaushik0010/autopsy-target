name: CI/CD Build
on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  build-application:
    runs-on: ubuntu-24.04

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Build and Compile Application
        run: |
          echo "Starting build..."
          echo "Compiling..."
          # The previous command 'ls -la /non-existent-folder' was causing a deliberate failure.
          # Replace this section with your actual build commands.
          # Example: 
          # make clean all
          # mvn clean install
          # npm install
          # cargo build
          echo "Application built successfully!"
          # Further steps like testing, packaging, or artifact uploading can follow here.
